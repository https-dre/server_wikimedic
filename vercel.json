{
    "version": 2,
    "build": {
        "src":"dist/Server.js",
        "use" : "@now/node"
      },
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/"
      }
    ],
    "buildCommand": "npm run build",
    "installCommand":"npm install"
  }